//SUPER QUERY FOR LOGIN

SELECT * FROM users INNER JOIN devices on users.id_user=devices.id_user LEFT JOIN tokens on tokens.id_device=devices.id_device WHERE users.id_user = 2

^KLAUZULA WHERE NA PODSTAWIE EMAIL I HASLA 

// DEVICE BIERZEMY, W LOGOWANIU NA STRONIE, Z HTTPONLY COOKIE JEZELI NIE ISTNIEJE TO GO TWORZYMY
// POTEM STANDARDOWO USTAWIAMY JWT JEZELI JEST TAKA MOZLIWOSC TO UPDATE ISTNIEJACEGO REKORDU JAK NIE TO NOWY
// NASTEPNIE USTAWIAMY COOKIES NA "NIESKONCZNOSC" 